Docker imagen for cowsay.
To build <br>
`sudo docker build -y cowsay-imagen .`
Run this imagen inside a container.<br>
`sudo docker container run cowsay-imagen dragon-and-cow "VQV TRYBE"`
Return should BE



